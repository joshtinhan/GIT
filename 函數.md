# 定義一個遞迴階乘函數
```JavaScript
    var mathFunc = function mathF(a){
        var res = a;
        a--;
        if(a>0){
            res *= matchFunc(a);
        }
        return res;
    };
    var mathRes = mathFunc(5);
    console.log(mathRes);//120
```